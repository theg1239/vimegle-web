import { TypeIcon as type, LucideIcon } from 'lucide-react';

export interface Feature {
  icon: LucideIcon;
  title: string;
  description: string;
  gradientClass: string;
  hoverMessage: string;
}

